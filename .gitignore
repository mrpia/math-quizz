node_modules
dist
dist-ssr
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log
.env
.env.*
*.tsbuildinfo
coverage
.superpowers/
.playwright-mcp/

# Playwright output: the HTML report and the per-test artefacts (traces,
# screenshots, saved downloads). All regenerated by `pnpm test:e2e`.
playwright-report/
test-results/
blob-report/

# Claude Code per-machine state. Ignored here rather than relying on a
# contributor's global excludes — it holds local paths and permission
# allowlists that are personal to one checkout.
.claude/settings.local.json
.claude/.cc-writes/

# Firebase CLI
.firebase/
*-debug.log
